xrr: "1"
adapter: exec
fingerprint: "58513353"
recorded_at: "2026-08-20T06:01:40Z"
payload:
    stdout: |
        feat(store)!: rename storage module to store

        Refactored module name from `storage` to `store` across all import paths. Updated all references to use the new module name.

        BREAKING CHANGE: Module `storage` is now `store`. Update all imports accordingly.
    exit_code: 0
    duration_ms: 6589
